{
  "name": "nblas",
  "version": "3.0.0",
  "description": "C++ bindings for all single- and double-precision CBLAS (Basic Linear Algebra Subprograms) routines.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "codegen": "python3 scripts/codegen.py",
    "docs": "typedoc --out docs --exclude \"**/*+(.spec).ts\" src && touch docs/.nojekyll",
    "build": "npm run codegen && node-gyp rebuild -j max",
    "prepublishOnly": "npm run codegen && rm -rf dist && tsc",
    "install": "npm run build"
  },
  "files": [
    "dist",
    "scripts/codegen.py",
    "src/index.cc",
    "routines",
    "lib"
  ],
  "nyc": {
    "extension": [
      ".ts",
      ".tsx"
    ],
    "exclude": [
      "**/*.d.ts",
      "dist/**",
      "docs/**",
      "coverage/**",
      "src/*.spec.ts",
      "src/index.cc"
    ],
    "reporter": [
      "lcov"
    ],
    "all": true
  },
  "homepage": "https://github.com/nperf/nblas#readme",
  "repository": "github:nperf/nblas",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1",
    "node-gyp": "^11.2.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^24.0.0",
    "jest": "^29.5.0",
    "prettier": "^2.8.7",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.1",
    "typedoc": "^0.28.18",
    "typescript": "^5.6.3"
  },
  "author": "Mateo Gianolio",
  "license": "MIT",
  "gypfile": true
}
